<%= form_with(model: vertex, local: true) do |form| %>
  <% if vertex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vertex.errors.count, "error") %> prohibited this vertex from being saved:</h2>

      <ul>
      <% vertex.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :vertex_nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
